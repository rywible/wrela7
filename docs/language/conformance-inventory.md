# Revision 0.1 conformance inventory

This is the authoritative implementation inventory for the normative prose in
chapters 01–10. It describes the advertised
`aarch64-qemu-virt-uefi` profile; it is not a conformance claim.

## Rules for maintaining this inventory

- A row covers every normative sentence and list item in the cited section(s).
  A section is split when its obligations have different implementation status.
- `verified` means the cited implementation is exercised through every applicable
  compiler/runtime layer and the cited evidence passes. `partial` means the row
  has real implementation and evidence but at least one named layer or behavior
  is absent. `gap` means there is no end-to-end implementation. `exclusion`
  means the normative specification itself explicitly places the feature outside
  revision 0.1; project scheduling is never an exclusion.
- Layer abbreviations are `S` source/package/parser/formatter, `A` semantic
  analysis and diagnostics, `SW` SemanticWir, `FW` FlowWir, `MW` MachineWir,
  `N` LLVM/ARM64 COFF/EFI, `R` target runtime/QEMU, and `P` image report/tooling.
  A dash in a layer list is an explicit missing layer, not “not applicable”.
- Evidence names checked-in files. A test name without a successful recorded run
  is implementation evidence, not proof of conformance. The focused gate for a
  row is `cargo xgate <owning-slice>`; milestone proof additionally requires
  the native `--full` gates against the system QEMU profile.
- A new normative heading or obligation must add or split a row in the same
  change. Unsupported source must fail closed with a source diagnostic; accepting
  and silently weakening a row is forbidden.

Unless a row narrows applicability explicitly, source-only rules require
`S,A,P`; compile-time-only rules require `S,A,SW,FW,MW,N,P` when they materialize
runtime data/code and `S,A,P` otherwise; every runtime-observable rule requires
`S,A,SW,FW,MW,N,R,P`; and image/product rules require all eight layers. Thus a
`partial` or `gap` row names missing end-to-end coverage even when its evidence
cell mentions only the layers currently present. “Not implemented”, “absent”,
or “none” is explicit evidence that no qualifying positive test exists; a row
may become `verified` only after replacing that statement with concrete positive,
negative, exact-limit/cancellation (where applicable), native, report, and QEMU
evidence.

The compiler phase map is documented in [the crate contracts](../../crates/README.md),
and versioned boundary fixtures are described in
[the contract fixture index](../../tests/contracts/README.md).

## Chapter 01 — Foundations

| Spec | Contract inventory | Layers/evidence | Status / delivery owner |
|---|---|---|---|
| 1.1 | Sealed fixed-function image, finite actor/task/resource world, one application core. | Model exists across build/target crates; no complete appliance/QEMU proof. | partial — req-02,03,12 |
| 1.2 | Exact reachable closure from one image, target ABI, handlers/tasks/ISRs/teardown and generated runtime; reject dynamic/unbounded closure. | A/SW/FW/MW scaffolding; `crates/wrela-compiler/tests/build_planner.rs`. | partial — req-09 |
| 1.3 | One address space/core/cooperative loop, bounded frames, ISR stacks, and no boot-services calls after transition. | Target/runtime ABI and EFI linker exist; recurring runtime proof absent. | partial — req-03,15 |
| 1.4 | Typed image graph, actor roles/wiring, capabilities, pools, IRQs, supervision and stable value handles. | Image/build models exist; general graph execution absent. | partial — req-03,08,09 |
| 1.5.1 | Owned fields, exclusive `mut`, initialized `take`, projection-only lexical views, explicit aggregate copy, legal actor payloads. | Scalar/flat aggregate/move fragments only. | partial — req-05 |
| 1.5.2 | Exactly-one bounded region, frame-safe suspension, linear cleanup, deterministic acyclic cleanup dependencies. | Analysis-tier free-call scopes produce exact `ScopeProtocol`/`ScopeActivation` facts and a checked `CleanupAcyclic` proof; `nested_scope_activations_form_reverse_source_cleanup_dag` proves lexical dependencies and reverse-source readiness, while `cleanup_cycle_detector_reports_synthetic_back_edge_participants` exercises the otherwise source-inexpressible back-edge detector. Parameterless, pass-only free-call scopes in actor turns lower to exact SemanticWir plans, authenticated Flow `GeneratedCleanup` helpers/calls, and an exact flat state argument through MachineWir. Direct unit returns commit and exit every directly nested active scope in proved inner-before-outer order. One direct condition may omit `else`, return from either explicit `if`/`else` branch, or return from both branches; SemanticWir inserts exact commit/exit pairs before every admitted return (`structured_scope_return_cleans_return_and_fallthrough_paths`, `if_else_structured_return_cleans_both_paths`). The five-block native profile admits either symmetric returning branch while the other continues to cleanup and suspend; `structured_scope_return_reaches_machine_and_deterministic_native_coff` authenticates that shape independently through MachineWir/codegen and covers substituted/deleted cleanup forgeries, exact Machine instruction N/N−1 bounds, late cancellation, and repeat identity. Arbitrary branched async callers, `elif`, loop/match exits, break/continue, failure/`?`, scope-held await, abort phases, and observable non-pass teardown effects remain absent or named fail-closed. | partial — req-05 |
| 1.5.3 | Actor ownership/non-reentrancy, acyclic unified wait graph, finite activations, checkpoints, irrevocable admitted takes. | One-way actor vertical exists: `actor_flow_vertical.rs`, `actor_one_way_send_vertical.rs`; cross-actor unit send: `actor_cross_send_vertical.rs`. The bounded scheduler prerequisite admits exactly one capacity-one, same-actor unit-message chain from startup to a first turn and then to a distinct terminal second turn; the two `SchedulerFifo` activations share one tag-dispatched image-entry drain and reach deterministic native COFF (`capacity_one_startup_to_turn_chain_is_admitted_non_reentrantly`, `capacity_one_recurring_chain_drains_two_unit_turns_to_native_coff`). The same vertical file now proves one narrower cross-actor typed reply: a sole startup-task await of a sole-path exact-`u64` actor result owns one 16-byte caller slot and reaches deterministic native COFF (`exact_u64_actor_reply_reaches_one_native_caller_owned_slot`). Longer chains, concurrent or parked replies, cycles, cross-actor recurrence, and a `runtime.S` scheduler remain absent. | partial — req-03,04,07 |
| 1.5.4 | Manifest capabilities, exclusive ISR, DMA/control-memory ownership, untrusted checks and quiescent cancellation. | Target types/tables are modeled; no complete virtio execution. | gap — req-08 |
| 1.5.5 | Closed dispatch, deterministic bounded comptime, value errors vs abandonment, restart after teardown. | Bounded comptime vertical exists; other clauses incomplete. | partial — req-09,10 |
| 1.6 | Enforce safety claim while accurately documenting compiler/runtime/firmware/device TCB; no general unsafe/FFI. | Architecture boundaries documented; full enforcement follows feature rows. | partial — req-18 |
| 1.7 | Static layouts and ceilings with bounded runtime occupancy and reported promotion. | Build/report models exist; general region accounting absent. | gap — req-05,09 |
| 1.8 | Only the explicitly listed later-revision features are excluded; multicore *runtime* on the advertised profile remains excluded while §8.1 states normative static-placement semantics. | See exclusion ledger below; placement model is documentation-verified. | verified — documentation |

## Chapter 02 — Source language

| Spec | Contract inventory | Layers/evidence | Status / delivery owner |
|---|---|---|---|
| 2.1 | UTF-8/Unicode 16 XID+NFC/security, `#` comments and `##` doc-comment attachment to the following declaration, four-space blocks, header continuations, literals/escapes/interpolation (`"""` reserved as a syntax error) and bounded formatting. | Syntax and formatter crates plus `tests/contracts/syntax/v3`. The scanner consumes a triple-quoted spelling as one recoverable token and emits exactly one `syntax-reserved-multiline-string` diagnostic over its opening delimiter (`triple_quoted_string_is_one_exact_reserved_syntax_error`). Runtime analysis admits unannotated, unconsumed synchronous locals containing plain text/byte literals and a bounded primitive interpolation subset. Plain text records exact UTF-8 bytes as compiler-minted `Static[Str]`; byte literals record exact decoded length as `Static[Bytes[N]]`. An interpolation with one or more core `bool`, character literals, or primitive integer values and no format specifier mints `BoundedString[capacity]`. Capacity is the checked sum of decoded literal UTF-8 bytes, five bytes per bool, four bytes per character, `digits(2^N−1)` for unsigned `uN`, or one sign byte plus `digits(2^(N−1))` for signed `iN`; `usize`/`isize` use the selected target width. Thus `f"ready={true}"` proves 11, a character proves 4, `u8` proves 3, and `i8` proves 4. `bounded_character_interpolation_uses_exact_utf8_width` and the existing bool/integer tests prove source-order witnesses, exact capacity, every fixed integer width, target-sized integers, and default `i64` literals. Full sealing rejects capacity, order, content, expression/value identity, primitive kind/type/width, effect, ownership, and duplicate-type substitutions (`bounded_character_interpolation_full_seal_rejects_witness_forgery`, `bounded_bool_interpolation_full_seal_rejects_witness_forgeries`, `bounded_integer_interpolation_full_seal_rejects_type_width_and_value_forgery`); bool, character, and integer type N/N−1 plus last-poll cancellation are pinned separately. Character support is intentionally literal-only; ordinary character locals and expressions remain outside this increment. Format specs, floats, other nonprimitive or unsupported computed values, ISR/async use, explicit contexts, and consumers fail closed by stable named diagnostics (`bounded_primitive_interpolation_tails_fail_closed_by_name`). Semantic lowering stops explicitly at `semantic-bounded-string-lowering-pending` (`bounded_primitive_interpolation_stops_at_named_semantic_lowering_boundary`). Unicode/security and the full literal corpus remain incomplete; `##` doc comments are not attached semantically; general `Format`, owned string/bytes operations, SemanticWir storage, ABI, and native representation remain open. | partial — req-02,13 |
| 2.2 | Module declaration/path identity, absolute imports/re-exports, visibility, SCC imports and semantic-cycle diagnostics. | Package/source/HIR pipeline exists; complete cycle/visibility matrix unproven. | partial — req-02,09 |
| 2.2.1 | Exact schema-1 manifest with no lockfile (dependency graph fully determined by `wrela.toml` plus the toolchain-shipped `core` component); modules derived by sorted `source_root` walk with verified module-declaration bijection (no `[[module]]` list); explicit dependencies/profiles/images/tests; default profile keys except `name`/`mode`; canonical acquisition and sole target. | `wrela-package*`, `tests/contracts/package/v1`. No lockfile in loader or fixtures: `crates/wrela-package-loader/src/loader.rs` resolves the root manifest's sole dependency aliased `core` directly against the toolchain's verified standard-library index (`root_manifest_with_more_than_one_dependency_is_rejected`, `root_manifest_dependency_not_aliased_core_is_rejected`, `canonical_loader_seals_root_and_core_workspace`); `std/examples/*` ship no `wrela.lock`; end-to-end proof in `crates/wrela-compiler/tests/build_planner.rs` and `crates/wrela-compiler/tests/headless_check.rs`. | partial — req-09,16 |
| 2.2.2 | Fixed shadowable prelude — `Option`, `Some`, `None`, `Result`, `Ok`, `Err`, `panic` — resolving without import via ordinary name resolution; every other name explicitly imported; scalar spellings builtin, not prelude bindings. | Implemented: ambient bindings injected after imports/module symbols in `wrela-hir-lower` (`inject_fixed_prelude`); `std/wrela-core-0.1/src/{option,result,panic}.wr`; Builtin `Option`/`Result` removed from unqualified name map. Evidence: `runtime_result_vertical` without `from core.result import`; `core_option_scalar_rvalue_question_propagates_none_and_yields_some_payload` uses unqualified `Option` and `Some` while sealing the exact core declaration; `workspace_loader_seals_the_checked_in_minimum_image_and_toolchain_core` proves the exact eight-module core surface, including the non-prelude `core.actor` taxonomy module. | partial — req-02,11 |
| 2.3, 2.3.1 | All declarations and the three function colors, including phase-neutral `fn` whose comptime legality is checked at each comptime call boundary; explicit async/task outcomes and prefix `await`. | Parser models much of the surface; general executable functions/async absent. | partial — req-02,04 |
| 2.3.2 | Struct construction/privacy/copy, the `linear` modifier as the sole uniquely-owned-aggregate form, the `copy struct` implicit-copy POD modifier (legal only for recursively scalar/copy-struct fields; explicit `copy` on one is a redundancy lint), optional `init` with partial-initialization/fallible-rollback, `implements`, actor-root structs, generative brands and zero-sized values (unifies the former separate class declaration). | Flat nongeneric structs execute in bounded subsets: `runtime_flat_structure_vertical.rs`, `comptime_aggregate_vertical.rs`. L2.1a adds plain one-level field replacement on an owned local: sema creates a fresh aggregate SSA value distinct from the scalar RHS, full sealing replays and authenticates the prior local/field/RHS/replacement, and SemanticWir v12/FlowWir v14 retain an exact `InsertField` (`local_flat_field_update_reaches_deterministic_native_coff`, `projected_local_assignment_defines_a_fresh_aggregate_value`). L2.2a retains same-block structs with two or more primitive scalar fields as aligned unpacked MachineWir v15 values with exact construct/copy/insert/extract operations and LLVM native emission (`unpacked_struct_operations_validate_exact_field_types_and_layout`, `two_field_flat_local_reaches_unpacked_machine_wir_and_deterministic_coff`); L2.2b carries those exact values across typed CFG edges and LLVM aggregate PHIs (`two_field_flat_local_crosses_cfg_join_and_emits_deterministically`). L2.2c admits exactly one owned two-field primitive-scalar structure argument into an ordinary synchronous function whose entire body is one scalar field projection and return. Machine lowering and LLVM codegen independently authenticate the exact source function, call, parameter/result types, unpacked layout, and reader body; `two_field_owned_argument_reaches_scalar_reader_and_deterministic_coff` proves FlowWir v14 codec identity, Flow forgery rejection, exact Machine instruction N/N−1, late cancellation, and repeat native identity, while `ordinary_two_field_reader_is_reauthenticated_and_forgery_fails_closed` rejects a structurally valid Machine reader-body forgery. L2.2d admits the symmetric result boundary: an ordinary synchronous function with exactly two primitive-scalar parameters constructs and returns one owned two-field flat structure, and an ordinary scalar-result caller immediately consumes that value. `two_field_owned_result_reaches_caller_and_deterministic_coff` proves FlowWir v14 codec identity, rejects an extra-computation Flow builder, pins the exact Machine instruction N/N−1 ceiling, observes late cancellation, and emits repeat-identical native COFF; `ordinary_two_field_builder_is_reauthenticated_and_forgery_fails_closed` independently rejects the corresponding structurally valid Machine builder forgery. The one-field-`u64` representation remains erased to bitcasts. Primitive stored-copy-scalar generic flat structures now retain their concrete specialization through exact actor SemanticWir structure, aggregate-construction, and field-projection operations (`generic_flat_structure_specialization_lowers_constructor_and_projection`). Lowering independently authenticates ordered unbounded HIR type parameters, every substituted source field, canonical flat layout, and cancellably compares nonempty structure fields during sealing; same-declaration substitution and emitted field-name/type forgeries are rejected. The shared source-type builder uses the same authentication, but this slice does not claim a generated-test or native vertical. Multiple/borrowed/mutable aggregate arguments, aggregate results outside the exact builder, non-reader/non-builder bodies, other field counts, compound/nested/indexed/tuple/nonlocal mutation, generic field update, nested generic aggregates, and views remain named fail-closed. The sole admitted actor-state exception is the canonical direct `self.value` read/plain-write/checked-add subset described in row 3.6; it does not make actor classes first-class aggregate values. `copy struct` enables implicit duplication (`copy_struct_permits_implicit_duplication`). A first A4 increment admits nongeneric ordinary structures whose nonempty public, attribute-free primitive-scalar fields are mirrored by `init(mut self, ...)` value parameters and assigned exactly once as `self.field = field`. `canonical_infallible_initializer_constructs_a_flat_scalar_value` proves semantic identity, initializer forgery rejection, exact fact bounds, and cancellation; `canonical_infallible_initializer_lowers_to_exact_aggregate` independently replays the protocol before aggregate lowering; `infallible_initializer_reaches_deterministic_native_coff` carries the one-field form through canonical FlowWir/MachineWir and repeat COFF emission when LLVM is enabled. Unsupported unused declarations also fail closed. Partial/fallible rollback, private/defaulted/attributed or nested fields, generic/linear/copy/zero-sized init, computed assignments/control flow, and actor-root init remain named diagnostics; nested copy-struct fields and redundant-`copy` lint remain incomplete. | partial — req-02,05 |
| 2.3.3 | Closed enums, exhaustive guarded patterns, leading-dot variant patterns with bare identifiers always binding, CamelCase variant constructors (lint, not formatter, flags lowercase), alternatives, `is`, payload access, `.variant` expression shorthand, tail-position value arms per §7.1, and fixed-array patterns. | Local 1–256 variant subset; `runtime_result_vertical.rs`. Mixed-arity variants (unit + one-scalar) resolve at the runtime type tier (all-unit uses a tag-only layout), and a fieldless leading-dot variant now constructs the unit value directly: `wrela-sema` `analyzer.rs::runtime_enum_admits_mixed_arity_unit_and_scalar_variants`, `runtime_enum_all_unit_type_resolves_and_constructs_leading_dot_variant`, `runtime_unit_variant_argument_list_fails_closed`, `runtime_enum_still_rejects_multiple_payload_fields`. Parenthesized/argument-list unit construction remains rejected as `semantic-runtime-enum-unit-constructor-shape`; multiple-field payloads remain rejected as `semantic-runtime-enum-payload-shape`. Heterogeneous per-variant scalar payloads (e.g. `small(u8)` / `large(u64)`) now resolve at the runtime type tier: each variant keeps its own copy-scalar payload type and the layout is a tagged union whose one payload slot is sized/aligned to the maxima across payload-bearing variants (`wrela-sema` `analyzer.rs::runtime_enum_admits_heterogeneous_scalar_payloads`, `runtime_enum_heterogeneous_payload_slot_sizes_to_widest`). Flat-struct nominal payloads (a nongeneric, all-scalar-field struct as a variant's single field, e.g. `filled(Detail)`) now also resolve at the runtime type tier and fold into the shared tagged-union slot with no layout-code change; constructing such a variant analyzes clean (`wrela-sema` `analyzer.rs::runtime_enum_admits_flat_struct_payload_and_layout_folds_struct`, `runtime_enum_flat_struct_payload_construction_analyzes_clean`). Flat structs are scalar-only and cannot transitively contain the enum. Nongeneric closed-enum nominal payloads (a nongeneric closed enum as a variant's single field, e.g. `some(Inner)`) now resolve recursively, fold their exact size/alignment into the shared tagged-union slot, and construct from an exactly typed inner enum value (`wrela-sema` `analyzer.rs::runtime_enum_admits_enum_payload_and_layout_folds_inner`, `runtime_enum_admits_scalar_bearing_enum_payload_and_folds_widest`, `runtime_enum_enum_payload_construction_analyzes_clean`). Qualified unit construction such as `Inner.a` is authenticated as the exact nested variant; the construction test also proves full sealing rejects changing that fact to same-typed `Inner.b`. A direct or transitive payload cycle (`more(Loop)`, or `A.x(B)` / `B.y(A)`) is detected structurally via a resolution-in-progress `DeclarationId` stack threaded on `RuntimeAggregateWork` and fails closed with `semantic-runtime-enum-recursive-payload` — never looping, overflowing the native stack, or leaning on a resource budget (`runtime_enum_rejects_direct_recursive_payload`, `runtime_enum_rejects_mutually_recursive_payload`). Exhaustive statement `match` now consumes mixed-arity nongeneric ADTs: unit arms bind nothing and scalar, flat-struct, or nongeneric-enum arms bind each variant's own semantic payload type (`runtime_adt_match_admits_unit_scalar_and_struct_payload_arms`, `runtime_adt_match_binds_each_variant_own_payload_type`). Guarded constructor arms, payload `_`, and one final whole-pattern `_` are analyzed with coverage contributed only by an unguarded arm; omission, a guarded catch-all, or an arm after an unguarded cover remains a named rejection (`guarded_adt_match_full_seal_requires_the_exact_guard_fact`, `guarded_match_requires_an_unguarded_cover`, `catch_all_wildcard_order_and_guard_fail_closed_stably`). For terminal-return matches over the uniform unary copy-scalar enum subset, ordered guards and one shared trailing wildcard default lower through exact SemanticWir/FlowWir/MachineWir and repeat-deterministic native COFF (`guarded_payload_and_trailing_wildcard_match_reaches_native_coff`, `guarded_wildcard_match_lowering_has_exact_limit_and_late_cancellation`). A guarded variant that relies on the catch-all as its only unguarded fallback remains named fail-closed as `semantic-match-wildcard-guard-fallback-lowering-pending` (`guarded_variant_falling_through_to_wildcard_stays_named_fail_closed`). One unguarded alternative group now lowers without duplicating its body: payload-free variants use the exact complement default, while payload-bearing variants that all expose the same primitive scalar type share one authenticated default-arm parameter and one Flow/Machine payload extraction (`one_unit_pattern_alternative_group_lowers_as_exact_complement_default`, `shared_payload_pattern_alternative_lowers_one_canonical_body`, `unit_pattern_alternative_group_reaches_flow_machine_and_deterministic_native_coff`, `shared_payload_pattern_alternative_reaches_flow_machine_and_deterministic_native_coff`). Multiple alternative groups, guarded alternatives, source-wildcard composition, mixed payload types/shapes, and fixed-array patterns remain named fail-closed or absent. Runtime `is` analyzes for a unit tag or for a payload tag ignored with `_` (`runtime_adt_is_tests_unit_variant_without_binding`, `runtime_adt_is_tests_payload_variant_with_wildcard`); success-dominated payload bindings remain fail-closed as `semantic-runtime-is-binding-pending` (`runtime_adt_is_payload_binding_fails_closed`). Generic enum specializations whose variants carry primitive copy scalars reach exact actor SemanticWir, including differing per-variant specialized payload types; construction and exhaustive `match` preserve each variant's own payload identity and sealing rejects type substitution (`uniform_copy_scalar_generic_enum_specialization_lowers`, `heterogeneous_copy_scalar_generic_enum_specialization_lowers_exactly`). A mixed unit-plus-unary specialization with one shared primitive payload type now executes end to end: SemanticWir v12 and FlowWir v14 make constructor payload presence explicit, FlowWire v14 encodes that presence canonically, and MachineWir v15 authenticates one payload-presence bit per tag before LLVM zero-initializes the shared slot for a unit constructor. `mixed_arity_generic_enum_specialization_lowers_exactly` covers exact type, tag, binding, N/N-1, cancellation, and sealing; `mixed_arity_generic_enum_reaches_exact_flow_machine_and_native_coff` constructs and matches both shapes, proves `[false, true]` machine tag authority, round-trips the codec, and emits byte-identical native COFF when LLVM is built. The three `closed_enum_accepts_exact_bounds_and_rejects_*_substitution` validator tests reject forged payload presence at every WIR tier. All-unit generic specializations now construct and exhaustively match as exact tag-only values: `all_unit_generic_enum_specialization_lowers_exactly` proves SemanticWir shape, exact N/N-1, cancellation, and sealing, while `all_unit_generic_enum_reaches_exact_flow_machine_and_native_coff` round-trips FlowWir/wire v14, authenticates a one-byte MachineWir v15 type with no payload slot, rejects payload construction/projection forgeries, and emits repeat byte-identical native COFF when LLVM is built. View, tuple, and array payloads remain rejected (`semantic-runtime-enum-payload-type`, `runtime_enum_rejects_view_payload_variant`), and Flow/machine lowering of heterogeneous scalar enums or nominal-payload enums remains deferred; the nongeneric semantic boundary remains `semantic-enum-heterogeneous-lowering-pending` / `semantic-enum-nominal-payload-lowering-pending`. | partial — req-02,06 |
| 2.3.4 | Static interfaces, orphan/non-overlap/coherence, direct dispatch, operators and unique `From` propagation. | Concrete `interface`/`impl I for T` support includes nongeneric interfaces and one exact primitive stored-copy-scalar specialization of an unbounded type-only generic interface per nongeneric flat receiver. Requirement-owned interface parameters substitute into direct parameter/result positions and must exactly match the concrete impl signature; bounded/const/region parameters and nominal/nested arguments fail closed by name (`concrete_generic_interface_specialization_dispatches`, `generic_interface_specialization_tails_fail_closed_by_name`). `core.ops` operator desugaring reaches direct impl calls through `analyze_operator_interface_binary`/`evaluate_operator_interface_binary`, exercised by `stdlib_time_scalar.rs`, `check_cli.rs`, and `local_check`. A bounded analysis-tier method-call substrate resolves a unique visible inherent or concrete-impl synchronous `read self` method on a named local/parameter whose type is a nongeneric flat scalar structure. The method may own ordered unbounded type parameters inferred as primitive stored-copy scalars from its expected result and typed explicit arguments; an interface requirement and its concrete impl method may declare alpha-equivalent parameters in the same order. Repeated instances deduplicate by the shared specialization key. Calls record the receiver separately from canonical explicit-argument bindings, apply the existing declaration-owned label rule after the receiver, and exact sealing independently authenticates receiver, target, visibility, name, ordered substitution, key, and bindings. Further evidence: `concrete_read_receiver_method_call_is_exactly_sealed`, `concrete_interface_impl_method_call_analyzes`, `generic_read_receiver_method_specializes_and_seals_exactly`, `concrete_impl_generic_method_specializes`, `generic_method_tails_fail_closed_by_name`, and `method_call_tails_fail_closed_by_name`. Generic enums separately specialize over concrete primitive stored-copy-scalar type arguments and retain that specialization through constructors and `match`/`is`; uniform and heterogeneous unary payloads reach exact actor SemanticWir (`uniform_copy_scalar_generic_enum_specialization_lowers`, `heterogeneous_copy_scalar_generic_enum_specialization_lowers_exactly`). This remains closed concrete dispatch, not bounded dispatch or overload resolution. Multiple specializations for the same interface/receiver remain rejected by coherence rather than selected from value arguments. Generic receiver structures, generic impl targets, bounded/const/region method parameters, async methods, mut/take or temporary/associated receivers, ambiguity and unknown methods remain named fail-closed. Read-receiver calls to an exact concrete or method-owned primitive-scalar specialization now lower to a direct SemanticWir call with the receiver first and declaration-owned explicit-argument order; `concrete_read_receiver_method_call_lowers_to_exact_semantic_call` proves receiver authority and seal rejection of a same-typed receiver substitution. Mutate/take receiver or explicit arguments remain named lowering boundaries; generic-interface body dispatch and native aggregate call execution remain absent. `implements`-clause bodies and generated `Eq` remain absent. | partial — req-02,06,09 |
| 2.3.5 | Projection carrier/implicit-conservative-provenance and scope enter/abort/exit; scopes cannot hold state across `await`. | Free-call scope and projection protocols retain exact identities, conservative provenance, and `ViewDoesNotEscape` authority. Structured backward liveness records per-path terminal references and exact live-after statement IDs across straight-line code, `if`/`else`, exhaustive enum `match`, branch-local return, and one outer canonical bounded `while` whose post-loop continuation already retains the view. That loop uses one invariant ephemeral view identity instead of fabricated header/exit SSA values; the body is analyzed once with the continuation live, and full sealing independently re-derives every retained statement (`continuation_anchored_bounded_while_keeps_view_live_exactly`). Source mutation and carrier rebinding in the loop interval fail before partial fact publication (`continuation_anchored_loop_keeps_projection_source_frozen`). Loop-local-only uses, nested loops, `break`/`continue`, and `with` remain named fail-closed (`lexical_view_loop_local_nested_and_transfer_tails_fail_closed_by_name`). Existing branch/match evidence remains `asymmetric_if_view_liveness_releases_the_nonretaining_branch`, `asymmetric_if_view_liveness_allows_nonretaining_await`, `asymmetric_match_view_liveness_uses_an_or_join`, and `branch_local_return_releases_a_view_needed_only_by_the_continuation`. B1b execution is narrower: one generated-test scalar projection reaches SemanticWir/FlowWir/MachineWir and repeat byte-identical COFF (`read_only_scalar_view_projection_reaches_semantic_wir`); structured-lifetime lowering, multi-source, receiver, wrapped, generic, mutable, deep/non-primitive, actor-image, and broader consumption remain named fail-closed. Scope receiver form and abnormal/non-pass cleanup remain fail-closed; the parameterless pass-only normal path reaches a real Flow cleanup call. | partial — req-05 |
| 2.3.6 | Closed `deriving(Eq, Format, From)` clause on struct/enum declarations; a name outside the compiler-known list is a build error; `From` legal only for a single payload-carrying field; generated semantics per chapter 10 §14. | Parse + HIR `deriving` names; sema rejects unknown names (`semantic-deriving-unknown`) and illegal `From` shapes (`semantic-deriving-from-shape`, including one variant with multiple fields). `deriving(Eq)` is executable for nonempty nongeneric flat structures whose fields are primitive stored copy-scalars. `==`/`!=` borrow both aggregates; the sealed fact records declaration-order field identities, both projections, every equality result, and every conjunction result. Lowering independently authenticates that chain, compares all field pairs eagerly, folds booleans deterministically with `BitAnd`, and applies one final `BoolNot` for `!=`. The local-aggregate form round-trips through FlowWir/MachineWir and emits repeat byte-identical COFF. Evidence: `runtime_flat_structure_vertical.rs` `multifield_derived_eq_reaches_deterministic_native_coff`, `multifield_derived_eq_full_seal_rejects_order_and_fold_forgeries`, `derived_eq_full_seal_rejects_field_and_intermediate_forgeries`, `derived_eq_lowering_has_exact_operation_bound_and_late_cancellation`, `derived_eq_rejects_unmarked_and_unsupported_shapes_by_name`, `deriving_from_rejects_a_single_multifield_payload_variant`, and `deriving_unknown_name_is_rejected`. Empty/generic/nested structures, enums, aggregate function boundaries, `Format`, and generated `From` conversion remain fail-closed or unimplemented. | partial — req-02,11 |
| 2.4, 2.4.1 | Exact read/mut/take call markers, declaration-owned argument labels (label-required when two or more non-receiver parameters; unary non-receiver parameter positional by default; `_` declares positional-only; exactly one legal spelling per call site), read-parameter-as-loan (never a copy), place operands, binding/evaluation order and explicit receiver effects. | Scalar/aggregate call fragments; general access analysis absent. Labels: syntax `_` marker, HIR `positional_only`, sema/comptime enforcement (`semantic-argument-label-required` / `label-forbidden` / `unknown-label`). Evidence: `wrela-syntax` `parses_positional_only_parameter_marker`; stdlib time unary call sites; `stdlib_time_scalar` / `stdlib_time_runtime_vertical`. | partial — req-02,05 |
| 2.5.1 | Primitive integer/float/unit/never semantics, checked/wrapping arithmetic/conversions, canonical NaN and target widths. | Integer verticals: `checked_shift_vertical.rs`, `compound_assignment_vertical.rs`; float completeness absent. | partial — req-02 |
| 2.5.2 | Tuples, fixed arrays, bounded collections/strings/bytes with the `..N` bounded-occupancy spelling (plain `N` spells exact extent), Static/Option/Result/Actor/iso/view/capability legality. | Flat structures and restricted `Result[S,S]`, plus analysis-only compiler-minted `StaticString { bytes }`, `StaticBytes { bytes }`, and `BoundedString { capacity }` types. Static literals are exact-kind interned explicitly-copyable immutable handles; `BoundedString` is reclaimable-linear and interns the exact checked interpolation capacity. All retain deliberately unknown size and no source-written ABI. Only inferred, unconsumed synchronous locals are admitted; `BoundedString` currently carries literal text plus canonical core-bool, character-literal, or primitive-integer decimal interpolation. Character capacity is the maximum four-byte UTF-8 scalar width; the character witness seals exact expression/value identity. The integer witness additionally seals semantic type and maximum decimal width; signed widths include a sign and target-sized integers use the selected pointer width. Ordinary character bindings/expressions, general owned `String`/`Bytes`, format specs, broader computed expressions, explicit contexts, consumers, persistence, ISR use, and all lowering remain absent or named fail-closed. Evidence: `bounded_character_interpolation_uses_exact_utf8_width`, `bounded_character_interpolation_full_seal_rejects_witness_forgery`, `bounded_character_interpolation_type_budget_is_exact_and_cancels_at_the_last_poll`, the existing integer/bool tests, `bounded_primitive_interpolation_tails_fail_closed_by_name`, and `bounded_primitive_interpolation_stops_at_named_semantic_lowering_boundary`. | partial — req-02,06 |
| 2.5.3 | Type/const generics, inference and monomorphization without runtime generics; region brands are compiler-managed with no surface `region` parameter in `generic_params`. | Analysis-tier type-only specialization now covers flat structures, module-level synchronous functions, synchronous read-receiver methods on nongeneric flat structures, enums, and one exact generic-interface application per receiver when every unbounded interface parameter is a primitive stored copy scalar. Requirement signature comparison substitutes interface-owned parameters into direct parameter/result positions before checking the concrete impl (`concrete_generic_interface_specialization_dispatches`, `generic_interface_specialization_tails_fail_closed_by_name`). Generic enum constructors require an exact expected specialization; exhaustive `match` and `is` preserve it, while full sealing independently re-derives type arguments, variant substitution, layout, and the distinct authenticated core `Result` contract. Evidence: `generic_enum_specializes_copy_scalar_payload_for_construction_match_and_is`, `generic_enum_rejects_constant_parameters_by_name`, `generic_enum_rejects_nominal_type_arguments_by_name`, `generic_enum_constructor_requires_expected_specialization`, and `generic_enum_nominal_payload_tail_fails_closed_by_name`. Function and method inference combine the expected result with already-typed explicit arguments, intern one exact source-function instance per ordered argument vector, deduplicate repeats, substitute parameter/result signatures, and share an active-specialization recursion guard. Method dispatch retains the separately authenticated concrete receiver and admits alpha-equivalent ordered unbounded type parameters on an interface requirement and its concrete impl method. Further evidence: `generic_flat_structure_specializes_copy_scalar_type_arguments`, `generic_copy_scalar_function_specializes_expected_types`, `generic_function_infers_from_a_typed_argument_without_result_context`, `generic_function_inference_conflict_is_named`, `generic_function_unconstrained_argument_is_named`, `generic_function_parameter_kinds_fail_closed_by_name`, `generic_function_recursion_fails_closed_by_name`, `generic_read_receiver_method_specializes_and_seals_exactly`, `concrete_impl_generic_method_specializes`, and `generic_method_tails_fail_closed_by_name`. Uniform and heterogeneous unary copy-scalar generic enums, primitive-scalar flat structures, and module-level synchronous generic copy-scalar function instances now lower into exact actor SemanticWir. Heterogeneous enum lowering sizes the semantic tagged-union contract from the widest specialized payload, preserves the per-variant type through construction and `match`, and seals against payload substitution (`heterogeneous_copy_scalar_generic_enum_specialization_lowers_exactly`). Function lowering independently authenticates module ownership, ordered unbounded type parameters, scalar arguments, every substituted parameter/result type, exact instance key, body return, and direct-call target; full sealing rejects key or call-target forgery (`generic_copy_scalar_function_specializations_lower_exactly`, `generic_function_lowering_tails_fail_closed_by_name`). Method-owned primitive-scalar generic specializations with a nongeneric flat read receiver lower to exact receiver-first SemanticWir calls (`concrete_read_receiver_method_call_lowers_to_exact_semantic_call`). The same SemanticWir path now has explicit evidence for a concrete impl selected through one primitive-copy-scalar generic-interface specialization: `concrete_generic_interface_dispatch_lowers_to_exact_semantic_call` authenticates receiver-first call order, rejects a same-signature target substitution in the full seal, pins exact operation N/N−1, and observes final-poll cancellation. Flow/native generic-interface dispatch is not claimed. The authenticated source `Result[T,E]` path remains constrained to identical primitive payloads. One compiler-privileged, analysis-only composition now re-derives exact `AsyncExit[u64]` and asymmetric `Result[u64, AsyncExit[u64]]` for a directly awaited non-actor helper declared `Result[u64,u64]`; source construction is forbidden and full sealing reauthenticates declaration-to-effective type identity (`fallible_direct_async_await_produces_authenticated_async_exit_for_nested_match`, `async_exit_effective_type_forgeries_fail_full_seal`). Mixed unit-plus-unary generic enums with one shared primitive payload type now cross SemanticWir v12, FlowWir/wire v14, MachineWir v15, and the deterministic native vertical (`mixed_arity_generic_enum_specialization_lowers_exactly`, `mixed_arity_generic_enum_reaches_exact_flow_machine_and_native_coff`); all-unit specializations construct and exhaustively match through the authenticated tag-only MachineWir v15 representation (`all_unit_generic_enum_specialization_lowers_exactly`, `all_unit_generic_enum_reaches_exact_flow_machine_and_native_coff`); const/bounded/region, non-type/non-scalar, nested nominal/view function, method, or interface arguments and unsupported signatures retain named boundaries. Generic receiver structures and impl targets, multiple generic-interface specializations requiring overload selection, general async generic functions/methods, generic structure field updates, Flow/native heterogeneous-enum execution and broader generated-test/native generic proof remain pending. | partial — req-09 |
| 2.6 | Let/inferred bindings, move/copy, definite initialization, assignment and partial-field state on all CFG edges. | Scalar locals/branches plus L2.1a plain one-level field replacement on an initialized owned nongeneric flat-structure local. The update is aggregate SSA replacement, not an unproved address store; exact sema and IR sealers reject RHS/result or field/type substitution. L2.2a executes that replacement for same-block two-or-more-field primitive-scalar locals through unpacked MachineWir v15 and deterministic COFF; L2.2b retains the aggregate across typed branch/join edges and LLVM PHIs. L2.2c carries one owned two-field local across the exact ordinary scalar-reader call boundary (`two_field_owned_argument_reaches_scalar_reader_and_deterministic_coff`). L2.2d carries one owned two-field primitive-scalar result from an exact ordinary synchronous two-parameter builder back to a scalar-reading caller (`two_field_owned_result_reaches_caller_and_deterministic_coff`). Machine lowering and LLVM codegen independently replay the builder and call shape; the vertical rejects extra-computation Flow/Machine forgeries, pins the exact Machine instruction N/N−1 ceiling, observes late cancellation, round-trips FlowWir, and emits repeat-identical COFF (`ordinary_two_field_builder_is_reauthenticated_and_forgery_fails_closed`). Evidence also includes `projected_local_assignment_defines_a_fresh_aggregate_value`, `projected_assignment_adjacent_shapes_fail_closed_and_live_views_stay_frozen`, `local_flat_field_update_reaches_deterministic_native_coff`, `two_field_flat_local_reaches_unpacked_machine_wir_and_deterministic_coff`, and `two_field_flat_local_crosses_cfg_join_and_emits_deterministically`. Partial-field initialization, projected compound assignment, nested places, parameters/`self` beyond the exact reader/builder profiles, view write-through, other aggregate results, and broader aggregate function boundaries remain open and fail closed. | partial — req-02,05 |
| 2.7, 2.7.1 | If/match/for/while/loop, return/break/continue/pass, the closed compiler-known iteration set (ranges, fixed arrays, standard-container operations; no user-defined iteration protocol), bounded sync loops, async checkpoints, and `match`/`if` as tail-position block expressions whose arms each diverge or produce one agreeing value. | `elif_vertical.rs` and `bounded_while_vertical.rs` prove the canonical unsigned finite `while local < literal` slice with mutation, nested `if`, `continue`, `break`, cyclic SSA, and machine CFG. One closed `for` form is executable: a non-taking half-open range whose bounds are nonnegative `u64`-representable integer literals. The exact child-body `u64` binding and regionless `ClosedRange` fact authenticate endpoints and checked-subtraction maximum trip count. Semantic lowering independently re-authenticates the HIR/fact/binding identities, emits one bounded scalar loop with an exact `< end` header and checked increment, and its sealer rejects an N−1 bound substitution (`closed_literal_range_lowers_to_exact_bounded_semantic_loop`). Descending/equal endpoints execute zero body trips through a one-header-check representation floor; exact operation N/N−1 limits and late cancellation are covered (`range_lowering_keeps_zero_trip_and_control_tails_explicit`, `range_lowering_has_exact_operation_limit_and_late_cancellation`). The Flow/Machine vertical retains the loop back edge and induction value, and repeat native emission is byte-identical when LLVM is built (`closed_literal_range_reaches_flow_machine_and_deterministic_native_emission`). Analysis sealing remains covered by `closed_literal_range_for_loop_analyzes_with_exact_u64_binding`, `closed_literal_range_zero_trip_and_exact_fact_limit_are_admitted`, and `closed_literal_range_analysis_honors_late_cancellation`. Inclusive/dynamic/taking ranges, fixed arrays, containers, user-defined targets, loop-binding consumption, outer loop-carried state, suspension, and early loop control remain named fail-closed (`closed_range_for_tails_fail_closed_by_name`, `range_lowering_keeps_zero_trip_and_control_tails_explicit`). Exhaustive statement `match` analyzes over mixed-arity nongeneric ADTs with exact per-variant bindings, guarded constructor arms, payload wildcards, and a trailing catch-all (`runtime_adt_match_admits_unit_scalar_and_struct_payload_arms`, `guarded_adt_match_full_seal_requires_the_exact_guard_fact`). `return match` rewrites every single-expression, value-return, or complete value-producing `if` arm to explicit returns and rejects an incomplete arm as `syntax-tail-match-arm-value`; the uniform unary copy-scalar enum subset reaches deterministic native COFF (`tail_position_match_and_inline_if_reach_native_coff`). This is a tail-return form, not a general first-class match expression: an arm may run preceding statements before its final value, but alternatives, fixed-array patterns, and binding a value from `match` remain absent or fail closed. | partial — req-02,04 |
| 2.8 | Complete precedence/evaluation/place/call/assignment/temporary teardown/arithmetic contract, including the loosest-binding inline `if`/`elif`/`else` expression with mandatory `else` and parenthesization rules. | Several scalar expression verticals; inline `if`/`elif`/`else` parses, types, and lowers for the no-elif scalar slice (`runtime_flat_structure_vertical.rs` `inline_if_expression_analyzes`); a parenthesized no-elif inline `if` also composes as the returned value of a `return match` arm through native COFF (`tail_position_match_and_inline_if_reach_native_coff`). Runtime `is` now produces a sealed bool expression fact for a unit ADT tag or a payload tag ignored with `_` (`runtime_adt_is_tests_unit_variant_without_binding`, `runtime_adt_is_tests_payload_variant_with_wildcard`); branch-local payload bindings and native lowering remain deferred. Elif lowering and general expression/teardown execution remain open. | partial — req-02,05 |
| 2.9 | Non-escaping/escaping closure capture modes, regions, bounds and async legality. | Surface only. | gap — req-02,05 |
| 2.10 | Complete attribute legality/phase/effects, including image/actor/task/hardware/layout/test/budget attributes, the verified cross-package `@comptime` marker, `@test(runtime)` tier forcing, and statement-position loop `@budget` (which absorbs the former `@uninterrupted`). | Test and selected runtime attributes only; `@comptime`, `@test(runtime)`, and statement-position `@budget` are not implemented. | partial — req-08,09,13 |
| 2.11 | Normative grammar accepts all legal forms and rejects all illegal productions with canonical formatting. | Syntax v3 fixtures exist; chapter-wide corpus incomplete. | partial — req-13,14 |

## Chapter 03 — Values, views, and regions

| Spec | Contract inventory | Layers/evidence | Status / delivery owner |
|---|---|---|---|
| 3.1–3.3 | Read/mut/take semantics, structural copy/linear classes and path-based exclusivity. | Restricted scalar/flat aggregate ownership only. | partial — req-05 |
| 3.4–3.4.6 | Ephemeral-type baseline (produced only by specific operations, consumed immediately, never stored/returned/captured/sent/held across suspension or checkpoint), lexical second-class views, projection carriers, implicit conservative provenance with named provenance diagnostics, disjointness, no escape/suspension and iterator access. | `wrela-sema` records a bounded lexical `ProjectionProtocol` using exact HIR parameter identities for conservative provenance, intentionally separate from allocation `RegionId`s. A regionless `LexicalView` retains exact parameter/value/span provenance, canonical per-path terminal uses, and an exact sorted statement-liveness witness. Structured backward analysis admits acyclic straight-line, `if`/`else`, exhaustive enum-`match`, and one outer canonical bounded `while` whose post-loop continuation retains the view. Joins OR arm liveness and use the continuation for an absent `else`, so only retaining arms remain frozen or barred from suspension; a nonretaining arm may mutate the former source or await. Mutation/rebound diagnostics resolve their terminal-use labels through the same branch-sensitive witness (`branch_local_mutation_names_its_own_blocking_terminal_use`, `branch_local_rebound_names_its_own_blocking_terminal_use`). A view with no later reference has exact empty terminal/live-after witnesses and releases at its initializer (`unused_projection_releases_at_the_end_of_its_initializer`). A bare return kills continuation liveness on that path, allowing a fallthrough-only terminal use without falsely retaining the return statement (`branch_local_return_releases_a_view_needed_only_by_the_continuation`); returning the view remains `semantic-view-escape`. Full sealing re-derives every structured witness. Loop-local-only uses, nested loops, `with`, `break`/`continue`, receiver/wrapped/generic projection calls, broader consumption, mutation through the view, projected-path disjointness, iterator access, and native lowering remain named fail-closed. | partial — req-05 |
| 3.5 | Generative branded `iso`, durable/request brands, actor transfer and ownership-conditioned recovery. | No general iso/pool runtime. | gap — req-05,07 |
| 3.6–3.6.5 | Image/task/call/request/iso region classes and exact lifetime/slot/reset rules. | The persistent actor-state subset admits exactly one private `value: u64 = 0` field on an installed actor plus direct `self.value` reads, plain `self.value = rhs` writes, and checked `self.value += rhs` inside that actor's mutable turn receiver. Analysis synthesizes one actor-owned `.state` `Image` region with exact eight-byte size/alignment and `CapacityBound(1)` proof. Each access carries a sealed `ActorStateAccess` joining the function, actor, receiver parameter, erased actor class, field, region, capacity proof, operation identity/value, and source; checked addition additionally authenticates distinct synthetic old-state and sum value identities against the exact assignment statement. SemanticWir v12 retains direct-write `Promote` → `ActorStateStore` and checked-add `ActorStateLoad` → checked `Binary(Add)` → `Promote` of the synthetic checked sum → `ActorStateStore`; the RHS is never the compound escape value. FlowWir v14/wire v14 carries both exact promotion proof markers immediately before their region/proof-authenticated `ActorStateAddress`/`Store` pairs. MachineWir v15 resolves those addresses to the existing `ActorState { actor }` global and retains an unsigned checked integer add; the FlowWir→MachineWir seal independently authenticates both marker/value/region/source adjacencies, erases only the markers, and retains the two concrete writes plus checked-add chain for the MachineWir→LLVM seal. LLVM emits repeat byte-identical ARM64 COFF. The actor class and source receiver remain erased—no receiver address or fake aggregate fields are introduced. Evidence: `zero_initialized_actor_state_has_exact_owned_storage`, `direct_actor_state_read_and_plain_write_are_exact_and_sealed`, `actor_state_checked_add_assignment_analyzes_and_seals`, `actor_state_other_compound_assignments_fail_closed_by_name`, `actor_state_tails_fail_closed_by_name`, `actor_zero_state_region_lowers_and_is_exactly_sealed`, `checked_actor_state_result_promotion_is_exact_bounded_and_cancellable`, `actor_zero_state_region_reaches_flow_and_is_exactly_sealed`, `actor_state_address_uses_appended_tag_and_roundtrips_authority`, `current_schema_accepts_only_exact_machine_wir_v15`, `actor_state_storage_no_longer_masks_later_plan_validation`, and compiler vertical `canonical_checked_add_actor_state_reaches_native_machine_storage`. Subtract/multiply/divide/remainder/bitwise/shift compound writes remain named fail-closed as `semantic-actor-state-compound-assignment-pending`; nested/indexed/multi-field places, other field types/counts, nonzero or missing initializers, multiple/app-state layouts, reset/lifetime inference, and general regions remain unsupported. | partial — req-05 |
| 3.7–3.9 | Whole-image region inference, reported promotion/budgets and bounded allocation/capacity errors. | Report schema v14 carries sealed `RegionAssignmentFact` and `PromotionFact` rows with dense `alloc:<u32>:<name>` identities and exact `region-bound` proofs. The bounded producer covers both a direct `self.value = rhs` store and the synthetic checked result of `self.value += rhs` in the canonical stateful actor cell: whole-image analysis classifies each escaped u64 value in the destination `Image` region, records its escape from the actor's non-reentrant `TaskFrame` region, and proves the eight-byte promotion. In the mixed vertical, source order yields `alloc:0:actor-state-store` and `alloc:1:actor-state-compound-store`; the compound promotion is the checked sum, never its RHS. Semantic lowering emits each authenticated no-result `Promote` immediately before its `ActorStateStore`; the SemanticWir sealer rejects substituted proof kinds, regions, or values. The compiler projection admits optional actor state regions and copies both exact assignment/promotion pairs into the public report, with exact item-limit, cancellation, and identity/reason substitution coverage. Bare `@no_promote` on the producing function, actor, or actor-state allocation rejects with `semantic-no-promote-escape` naming the exact allocation and selected build profile; compound-only evidence names `alloc:0:actor-state-compound-store`, while a nonpromoting function satisfies the constraint. Evidence: `direct_actor_state_read_and_plain_write_are_exact_and_sealed`, `actor_state_checked_add_assignment_analyzes_and_seals`, `checked_actor_state_result_promotion_is_exact_bounded_and_cancellable`, `no_promote_rejects_the_exact_actor_state_escape_and_names_the_profile`, `no_promote_on_a_nonpromoting_function_is_satisfied`, `parsed_actor_pipeline_projects_exact_nodes_capacities_sources_and_proofs`, `actor_projection_sealer_rejects_identity_proof_and_capacity_substitution`, and `actor_projection_enforces_exact_item_limit_and_midflight_cancellation`, in addition to the schema round-trip tests. L2.3 carries both exact promotions through FlowWir/wire v14 as proof-bearing markers and reauthenticates marker→actor-state-address→store adjacency in Machine lowering; both markers erase because the following stores are the concrete runtime actions. The single-marker Flow primitive retains exact N/N−1 and late-cancellation coverage; the two-marker compiler vertical separately pins the exact post-erasure 14-instruction Machine census, rejects an RHS-forged compound marker, round-trips canonically, and emits repeat byte-identical COFF. Evidence: `actor_state_promotion_reaches_flow_with_exact_region_authority`, `promotion_marker_retains_its_exact_region_bound_proof_reliance`, and `canonical_checked_add_actor_state_reaches_native_machine_storage`. This remains deliberately narrower than general allocation inference: other compound state writes, aggregates, arenas/pools, `@budget`, `Allocate`, `RegionReset`, capacity diagnostics, and other region classes/shapes remain deferred or fail closed. | partial — req-05,09 |
| 3.10 | `SlotMap[T, ..N]` graph model with fresh non-wrapping instance IDs, `Key` as a `copy struct` over `(map_id, index, generation)`, generation retirement, checked lexical access. | Not implemented end to end. | gap — req-06 |
| 3.11 | Universal `with`, abort/exit, deterministic cleanup dependency graph and all normal/abnormal teardown paths. | B4a analysis supports module-level free-call scopes used directly by `with`: exact `Scope`/`ScopeCall` resolution, lexical binding lifetime, pass-only non-suspending abort/exit validation, canonical nested cleanup dependencies, reverse-source ordering, and `CleanupAcyclic` facts (`free_scope_call_builds_protocol_activation_and_lexical_binding`, `nested_scope_activations_form_reverse_source_cleanup_dag`, `scope_abort_await_is_a_named_rejection`, `scope_exit_await_is_a_named_rejection`). The parameterless/pass-only actor-turn normal path crosses SemanticWir→FlowWir→MachineWir as an authenticated `GeneratedCleanup` call and native aggregate boundary. Direct unit returns from a scope body or directly nested scope chain clean before returning. One direct condition may omit `else`, return from either explicit branch, or return from both; `if_else_structured_return_cleans_both_paths` pins exact SemanticWir cleanup, operation N/N-1, late cancellation, and else-path forgery rejection. The native five-block profile now accepts the returning cleanup in either branch and the other branch's fallthrough-to-suspend cleanup. `structured_scope_return_reaches_machine_and_deterministic_native_coff` independently authenticates that symmetric shape in Machine lowering, MachineWir, and LLVM with cleanup/proof/state forgery rejection, Machine N/N−1 bounds, late cancellation, and repeat-byte identity. This does not admit general branched async activation callers. `elif`, nested conditional combinations beyond the prior bounded case, loops/matches, parameterized acquisition, abort phases, break/continue, `?`/failure, scope-held await, receiver scopes, non-pass cleanup, and event-stream teardown observation remain named fail-closed. | partial — req-05,10 |

A3 evidence for rows 2.3.3 and 2.7 includes one unguarded pattern-alternative
group over distinct constructors of the exact scrutinee enum. Every constructor
contributes exact exhaustiveness coverage and the full sealer replays the group
from HIR (`runtime_adt_match_unit_alternatives_contribute_exact_exhaustive_coverage`,
`runtime_adt_match_alternatives_share_one_exact_payload_binding`). Payload-free
groups lower canonically as the default arm after exact explicit arms cover the
complement. Payload-bearing groups also lower through that one default arm when
every covered variant exposes the same primitive scalar type and the same HIR
local: the arm has one authenticated parameter, its body is lowered once, and
Flow/Machine perform one payload extraction in the default block. Exact
operation N/N-1, late cancellation, and default-binding forgery rejection are
pinned by `one_unit_pattern_alternative_group_lowers_as_exact_complement_default`
and `shared_payload_pattern_alternative_lowers_one_canonical_body`. Both forms
reach exact FlowWir/MachineWir switching and repeat byte-identical native COFF
(`unit_pattern_alternative_group_reaches_flow_machine_and_deterministic_native_coff`,
`shared_payload_pattern_alternative_reaches_flow_machine_and_deterministic_native_coff`).
Multiple alternative groups, coexistence with a source wildcard, guarded
fallback overlap, mixed unit/payload groups, differing payload types, wildcard
payloads, and `take` remain named fail-closed
(`multiple_pattern_alternative_groups_stay_named_fail_closed`,
`pattern_alternative_default_composition_tails_fail_closed_by_name`,
`runtime_adt_match_alternative_tails_fail_closed_by_name`). Fixed-array and
broader alternative forms are not claimed.

A3 `is` evidence for rows 2.3.3 and 2.8 now carries an explicit authenticated
`EnumTypeTest` expression resolution: the exact semantic enum type, variant
index, and borrowed scrutinee value are sealed independently of the boolean
result. `runtime_adt_is_tests_unit_variant_without_binding` proves that changing
the fact to another valid variant is rejected by full sealing; payload-wildcard
analysis remains covered by `runtime_adt_is_tests_payload_variant_with_wildcard`.
`authenticated_enum_type_test_lowers_to_canonical_boolean_match` independently
reauthenticates those facts, pins exact SemanticWir operation N/N-1 and late
cancellation, rejects a selected-truth-arm forgery, and lowers both unit and
payload variants to an exhaustive boolean-result match without payload reads.
Flow accepts only the exact discriminating truth pattern and rejects a
nondiscriminating forgery
(`enum_type_test_protocol_rejects_nondiscriminating_truth_arms`). Positive and
negated tests reach exactly one tag read and no payload read apiece through
FlowWir, MachineWir, and repeat byte-identical native COFF
(`enum_type_tests_reach_tag_only_flow_machine_and_native_coff`). Contextual
payload bindings introduced by an `is` pattern remain analysis-only and are not
claimed executable.

B1a.2 retention-boundary evidence additionally includes
`continuation_anchored_bounded_while_keeps_view_live_exactly`,
`lexical_view_loop_local_nested_and_transfer_tails_fail_closed_by_name`,
`live_projection_source_and_view_carrier_cannot_be_rebound`, and
`projection_source_may_rebind_after_terminal_view_use`. Branch-local mutation
and rebound diagnostics label the exact terminal use on the conflicting path
(`branch_local_mutation_names_its_own_blocking_terminal_use`,
`branch_local_rebound_names_its_own_blocking_terminal_use`);
`non_unary_projection_terminal_use_remains_named_and_fail_closed` proves that
broader ephemeral consumption stays named and closed. The positive vertical
also forges retained-source rebinding in otherwise valid HIR and verifies that
the exact semantic sealer independently rejects it.
The direct-root disjointness proof additionally forges two individually exact
views onto one root and verifies that full sealing rejects their overlapping
exclusive intervals.

T0.2 classifies typed semantic values as first-class or as one of four closed
ephemeral carrier kinds. Exact prefix/full sealing authenticates real lexical
views and the bounded `try send` AdmissionResult producer, while rejecting a
view forged as first-class, producerless ephemeral data, and substitutions of
the AdmissionResult carrier class, nominal type, capacity proof, or actor-turn
method. `postfix_question_rejects_real_lexical_view_producer` and
`admission_result_try_send_question_and_other_consumers_fail_closed` exercise
the shared `semantic-ephemeral-question-forbidden` path; the ownership-
conditioned actor-call kind remains modeled but unproducible. The policy matrix
is pinned by `ephemeral_consumption_policy_is_closed_per_carrier`.

## Chapter 04 — Actors and async

| Spec | Contract inventory | Layers/evidence | Status / delivery owner |
|---|---|---|---|
| 4.1–4.2 | Actor roots/typed handles and legal fixed-layout payloads; handles remain image-wired; actor-associated `brand` aliases (`Owner.Brand`) bound exactly once by image construction. | Image-wired typed handles and unit payloads: `actor_flow_vertical.rs`, `actor_one_way_send_vertical.rs`, `actor_cross_send_vertical.rs`. Typed call payloads and brand aliases remain absent. | partial — req-03 |
| 4.3 | Non-reentrant turns: an actor holds its turn across `await` and no second external message re-enters a suspended turn; internal `self` calls are ordinary. | Non-reentrancy across `await` and the view-across-await frame rule are proven for the installed-actor slice (`analyzer.rs::parsed_actor_image_proves_non_reentrancy_async_frames_and_capacities`, `semantic-view-across-await`). One exact capacity-one chain may republish from its first turn to a distinct terminal second turn; the republish is statically before the fixture's existing immediate-`await` boundary. Sema reauthenticates the finite same-actor chain and rejects longer/cyclic shapes as `semantic-actor-send-chain` (`capacity_one_startup_to_turn_chain_is_admitted_non_reentrantly`, `recurring_chain_rejects_non_capacity_one_and_cycles_stably`). This slice does not prove executable suspension/resumption or re-entry around a suspended recurring turn. | partial — req-03,07 |
| 4.3.1 | Concrete unified wait-for graph over the closed image (turn locks, tasks, internal async activations, resources/producers) proven acyclic; wait cycles and driver self-waits are named rejections; driver handlers never self-wait. | Sema builds the wait-for graph over the closed image and proves its acyclicity (`ProofKind::WaitGraphAcyclic`): a hold-and-wait cycle among reachable async activations is the named `semantic-wait-cycle` rejection listing every participant, and a public `@driver` handler that self-waits (suspends at `await`) is the named `semantic-driver-handler-waits` rejection decided at declaration granularity — before image evaluation and independent of the driver-install fail-closed. Evidence: `wait_for_graph_vertical.rs` (`hold_and_wait_cycle_is_a_named_wait_cycle_rejection`, `driver_handler_self_wait_is_a_named_rejection_before_image_evaluation`, `acyclic_wait_graph_is_accepted_with_a_wait_graph_acyclic_proof`), `analyzer.rs::public_driver_handler_that_self_waits_is_rejected_before_image_evaluation`, `analyzer.rs::parsed_async_wait_cycle_is_a_structured_exact_span_diagnostic`. The exact startup-task→actor reply edge is now source-expressible and included in the sealed wait-graph inputs; actor-turn→actor-turn reply cycles and device/timer producer edges remain unexpressed pending broader reply scheduling and hardware resources. | partial — req-03,07 |
| 4.3.2 | One-way `send`/`try send`, reserve-before-evaluation, arm-sensitive ownership, restart capacity/FIFO. | Bounded executable `send`: `actor_one_way_send_vertical.rs` and diagnostics test; `actor_cross_send_vertical.rs` proves exactly one image-wired `@app`→`@service` unit send through native AArch64 COFF. The same-actor scheduler prerequisite extends only the capacity-one unit-message profile to startup→first turn→distinct terminal turn: SemanticWir, FlowWir, and MachineWir retain exactly two reserve/commit/receive edges, MachineWir authenticates the two distinct `SchedulerFifo` callers and one shared dispatch, and LLVM emits a deterministic tag-switched drain (`capacity_one_recurring_chain_drains_two_unit_turns_to_native_coff`). The analysis-only `try send` increment remains limited to one startup-task attempt, creates a separate `CapacityBound(1)` proof depending on the mailbox proof, and produces the exact ephemeral core `AdmissionResult` only as a direct exhaustive `match` or direct `is`; `?` is `semantic-ephemeral-question-forbidden` and storage/return/bare consumers are `semantic-admission-result-consumer-pending`. Evidence: `admission_result_try_send_matches_exhaustively_with_exact_ephemeral_facts`, `admission_result_try_send_supports_direct_is_for_both_tags`, and `admission_result_try_send_payload_attempt_bound_and_cancellation_fail_closed`. Typed one-way payloads, ownership-conditioned moves, recurring `try send`, runtime admission dispatch, and general recurrence remain absent; the separate exact-`u64` awaited-reply slot does not widen `send` or `try send`; `try send` lowering fails closed as `semantic-admission-result-lowering-pending`. | partial — req-03,07 |
| 4.3.3–4.3.4 | Observable admission/FIFO/turn/replay semantics and exactly-once typed replies/peer failures/take outcomes. | The bounded capacity-one two-turn unit-message chain has an exact FIFO caller set and deterministic native dispatch (`capacity_one_recurring_chain_drains_two_unit_turns_to_native_coff`). The first typed-reply subset admits only one direct startup-task await of one installed actor method with no arguments and one definite exact-`u64` return. `ActorReplyExactlyOnce` depends on the target type proof and capacity permit; request/resolve cross SemanticWir v12, FlowWir/model+wire v14, MachineWir v15, and LLVM. The caller owns one exact 16-byte/8-aligned slot whose atomic states detect mismatched or duplicate resolution with append-only runtime fatal codes 7/8. `exact_u64_actor_reply_reaches_one_native_caller_owned_slot` covers proof/slot/duplicate-resolve forgeries, the exact 16-byte stack ceiling, and repeat byte-identical COFF. The backend fuses this sealed same-core profile to an exact direct call; it does not claim parking, resumption, peer-failure/take outcomes, replay logging, typed parameters or broader results, multiple/concurrent replies, asynchronous dispatch, or a general runtime scheduler. | partial — req-03,07,10 |
| 4.3.5 | Single outcome taxonomy defined once: sealed `AsyncExit[E]`, composing `ActorCallError[E]` (`Exit`/`PeerFailed`/`NotAdmitted`), and ephemeral `AdmissionResult` sharing one `AdmissionError`; no other section restates the variants. | `core.actor` now carries compiler-authenticated public zero-size `Cancelled`, `DeadlineRejected`, and `DeadlineExceeded` causes plus ordered `AsyncExit[E] = Operation(E) | Cancelled(Cancelled) | DeadlineRejected(DeadlineRejected) | DeadlineExceeded(DeadlineExceeded)`. For the exact analysis-only profile, awaiting a direct non-actor async helper declared `Result[u64,u64]` yields internal ephemeral `Result[u64,AsyncExit[u64]]`, consumed only by an immediate exhaustive nested match or direct `is`; source construction, `?`, storage/bare consumers, other payloads, and semantic lowering fail closed by name. Evidence: `fallible_direct_async_await_produces_authenticated_async_exit_for_nested_match`, `fallible_direct_async_outcome_supports_direct_is`, `async_exit_core_shape_is_authenticated_exactly`, `async_exit_source_construction_is_forbidden_by_name`, `async_exit_consumer_tails_fail_closed_by_name`, `async_exit_effective_type_forgeries_fail_full_seal`, `async_exit_analysis_has_exact_type_limit_and_late_cancellation`, and `fallible_async_outcome_stops_at_named_semantic_lowering_boundary`. The existing exact `AdmissionError`/`AdmissionResult` analysis producer and consumers remain supported, and the checked-in core digest is pinned by `checked_in_bootstrap_manifests_are_schema_one_and_derive_stable_modules`. `ActorCallError[E]`, runtime cancellation/deadline delivery, actor failure composition, and native lowering remain absent. | partial — req-03,04,07 |
| 4.4 | Derived finite mailbox/turn bounds and logical-vs-physical reporting. | Build models only. | gap — req-03,09 |
| 4.5–4.5.1 | Ahead-of-time bounded async machines, static tasks, explicit exits, recursion/frame/stack bounds. | The bounded B6 execution profile delivers immediate typed async results without inventing a scheduler. A non-reentrant actor turn and one single-slot static task may each perform exactly two sequential awaits of the same ordinary async helper. Each caller retains a sealed three-block state chain: state 0 resumes into the sole state-1 call block, and state 1 resumes into an empty return block; four distinct 16-byte activation regions keep source/proof authority per suspension while maximum-live remains one. The computed `u64` helper may now take one exact `u64` literal argument, consume it through the retained parameter bridge, define one constant RHS, and perform one or two unsigned checked additions. The earlier no-argument unit/constant/seeded-computation forms remain admitted. FlowWir v14 retains every argument, strict-linear activation token, and explicit resume value. Machine lowering, MachineWir v15, and LLVM independently authenticate argument arity/type/value identity, both ordered states, the parameter bridge, and every checked-add/result/callee/jump link while erasing only token/resume plumbing. `parsed_actor_source_delivers_exact_u64_computed_async_results_to_actor_and_task` rejects two arguments, substitution of a dominating same-typed value for the adjacent literal, duplicate states, skipped-middle resumes, extra helper tails, swapped operands, non-unsigned results, and Machine result/type/bridge/jump/callee/source/proof forgeries; it pins exact two-state and instruction N/N−1 bounds, cancellation, codec passage, repeat Machine identity, and repeat COFF bytes. More than one argument, non-literal arguments, more than two awaits, non-sequential suspension CFGs, parked wakeup, cancellation execution, broader arguments/results, recursion, and general scheduling remain fail-closed. | partial — req-04 |
| 4.6–4.7 | Suspension legality, consuming awaitables, completions, idempotent wake and mask-arm-recheck park. | Runtime timeout slice is not general async: `runtime_timeout_vertical.rs`. | gap — req-04 |
| 4.8–4.10 | Normative deterministic scheduler, priorities/fairness proof, budgets/checkpoints and inherited deadlines. | A generated image-entry drain deterministically dispatches the exact bounded capacity-one two-turn unit-message profile, and one sealed same-core exact-`u64` reply is backend-fused to a direct call with an authenticated caller slot. Both are only L2.4 prerequisites. `runtime.S` has no scheduler; priorities, fairness, budgets, checkpoints, deadlines, parked replies, and general recurrence remain absent. | gap — req-04 |
| 4.11 | Bounded nursery/structured task creation and teardown. | Not implemented. | gap — req-07 |
| 4.12–4.12.3 | Ambient request lineage implicitly carried by every `async fn` (no signature threading; explicit `request=` override and `@detached` opt-out; tooling displays the inferred lineage), fresh lineage/brand minting, atomic child registration, cancellation cleanup, permit backpressure and sealed race/select. | Not implemented. | gap — req-07 |
| 4.13–4.14 | Single merged sealed `Receipt[P]` state machine (`Submitted`/`Committed`/`Resolved`/`Recovery`) with the `@receipt_handoff` proxy contract, device receipt throughput accounting, IRQ/poll idle behavior, and the normative **service slot idiom** (bounded in-flight `SlotMap` + sealed `slot.resolve(take receipt)` ending the service turn); head-of-line diagnostic repair names the idiom. | Not implemented end to end. | gap — req-07,08 |
| 4.15 | Normative multicore static placement (per-core schedulers, generated cross-core SPSC rings, IRQ/DMA affinity, admission-order replay); advertised 0.1 runtime remains single-core. | Design semantics documented; 2-core vertical absent. | gap — req-18 |
| 10.5.1 | Sealed `Slot.resolve(take receipt)` contract: parks reply, ends service turn, wires exactly-once receipt completion to a later turn with ownership-conditioned outcomes. | Not implemented. | gap — req-07,11 |

## Chapter 05 — Hardware safety

| Spec | Contract inventory | Layers/evidence | Status / delivery owner |
|---|---|---|---|
| 5.1–5.3 | Manifest-minted linear authority (`@driver` takes no device argument; the device is named once at the image binding), role/effect checking and nonoverlapping typed MMIO with volatile ordering. | Target/runtime ABI models only. | gap — req-08 |
| 5.4–5.5 | Exact DMA/wire layout, transfer vs shared-control memory, pool provenance/coherency/confinement. | Layout structures exist; ownership protocol execution absent. | gap — req-08 |
| 5.6–5.8 | Complete queue reservation, nonwrapping IDs/epochs, protocol ordering and the evidence-wrapper family — `Untrusted[T]` (taint-in) with `Validated[F, T]` (proof-out, chapter 10) counted as one family — gating checked control values. | Not implemented end to end. | gap — req-08 |
| 5.9–5.11 | Exclusive vector topology/GICv3 route, restricted ISR effects, InterruptCell ordering/masking and stack bounds. | Runtime ABI cross-contract test: `crates/wrela-runtime-abi/tests/cross_contract.rs`; source/effect/runtime completeness absent. | partial — req-08 |
| 5.12–5.14 | Bounded bottom halves, const-generic IRQ/poll/hybrid specialization and typed virtio protocol states. | Not implemented end to end. | gap — req-08 |
| 5.15 | Cancellation transfers receipts to bounded recovery, resets/quiesces, quarantines DMA and reports unknown outcomes. | Not implemented end to end. | gap — req-08,10 |
| 5.16 | Preserve and report residual compiler/target/firmware/hardware TCB. | Documentation present; distribution/report proof incomplete. | partial — req-08,16 |

## Chapter 06 — Comptime and images

| Spec | Contract inventory | Layers/evidence | Status / delivery owner |
|---|---|---|---|
| 6.1–6.5 | Typed target-emulated comptime, deterministic/no ambient I/O, finite quotas, safe phase crossing and nonobservable Secret taint; cross-package comptime calls require the `@comptime` marker, verified against the declaring package's transitive closure. | Bounded scalar/flat-struct evaluator vertical; Secret/general value surface and the `@comptime` marker absent. `comptime_unit_vertical.rs`, `comptime_aggregate_vertical.rs`. | partial — req-09 |
| 6.6–6.7 | Complete specialization, direct closed dispatch, graph-changing const configuration and phased hygienic attributes. | Restricted specialization only. | partial — req-09 |
| 6.8–6.9 | Unique typed image construction, graph roles/order/brands/restart provisions and exclusive capability minting. | Image/build models exist; complete source construction/runtime boot absent. | partial — req-09 |
| 6.10 phases 1–3 | Parse/collect, resolve/type comptime, evaluate image root under quotas. | Implemented for bounded subsets. | partial — req-09 |
| 6.10 phases 4–6 | Fixed-point specialized checking, graph invariant closure and exact resource inference. | Models/subsets only. | gap — req-09 |
| 6.10 phases 7–9 | Seal SW, optimize verified FW, lower MW/layout/report, read-only assertions, emit/link/reconcile. | Pipeline exists for selected verticals; general semantic and report reconciliation absent. | partial — req-09,15 |
| 6.11 | Content-addressed comptime caching with identical diagnostics and no unstable host input. | Cache tests: `artifact_cache_reuse.rs`, `change_set_reuse.rs`, `incremental_session.rs`; general evaluator equivalence incomplete. | partial — req-09,17 |
| 6.12.1 | Genuine bounded comptime-tier `@test fn` with tier selection by comptime-legality (`@test(runtime)` forces the runtime tier), exact-limit/cancellation diagnostics and canonical CLI reports for the documented subset. | `comptime_unit_vertical.rs`, CLI/test-model infrastructure. Implemented subset: a non-generic, module-level, zero-argument `@test fn` returning `unit` that calls scalar/flat-nongeneric-struct `fn`s using typed/inferred locals, scalar-field projection, named construction, flat-aggregate move/copy, branch-definite-init, returns, nested calls, integer/boolean operations, comparisons, and `comptime assert`; `@test(runtime)` is accepted by HIR and forces the runtime tier in sema (`declaration_forces_runtime_tier`). Excludes linear/actor-root construction, receiver/associated methods, generics, non-flat aggregates, floating point, and `Result`/`Ok`/`Err` in the comptime subset. | partial — section itself explicitly narrows delivery; req-13 expands it |
| 6.12.2 | Generated bounded runtime/image test harness (including tests routed there by `@test(runtime)`), protocol and pinned target execution. | Test protocol/runner and QEMU smoke exist; `@test(runtime)` routing is implemented in sema and used by std examples (`runtime-timeout`, `checked-shift-runtime`, `stdlib-time-runtime`, `stdlib-time-scalar` runtime case). General source-test surface still incomplete. | partial — req-13,14 |
| 6.13 | Deterministic ARM64 PE32+ EFI, exact UEFI transition/runtime entry and artifact/report match. | Native/link/test runner slices exist; complete current-tuple proof absent. | partial — req-15 |
| 6.14 | Atomic compiler/target/library contract and exact target rejection. | Toolchain/target crates and contract fixtures. | partial — req-15,16 |

## Chapter 07 — Faults and reliability

| Spec | Contract inventory | Layers/evidence | Status / delivery owner |
|---|---|---|---|
| 7.1–7.4 | Exact failure taxonomy, Result/task/async values, uncatchable abandonment and build-time assertions. | Restricted Result and assertions only. | partial — req-06,10 |
| 7.5–7.7 | Complete supervision tree, bounded restart intensity, zero-allocation teardown/provisioning and partial-state recovery. | A bounded static-topology floor now emits one exact `SupervisionComplete` proof for actor images: the current source producer roots installed actors at runtime and owns every bare `@task` by exactly its declaring actor. The proof binds the ordered actor/task source census, exact node bound, `TypeChecked` dependency, generated entry, and image closure. The sema full seal independently walks the bounded actor-parent forest, replays each task declaration's owning actor from HIR, and authenticates the proof sources, entry attachment, and closed-image reachability. SemanticWir and FlowWir validate the more general existing acyclic actor forest plus task ownership, so the existing report-only nested actor edge remains authenticated; FlowWir/codec and reports preserve the existing proof kind without a schema bump. The executable MachineWir profile remains root-actor-only and nested actor parents stop at `machine-supervision-policy-lowering-pending (nested actor parents)` because no machine parent record exists. Full sealers reject parent cycles/substitution, source, bound, dependency, kind, entry, and closure substitution (`static_supervision_full_seal_rejects_topology_and_closure_substitution`, `parsed_actor_image_proves_non_reentrancy_async_frames_and_capacities`, `parsed_actor_source_delivers_exact_u64_computed_async_results_to_actor_and_task`, `v6_actor_activation_capacity_contract_rejects_substitution_and_overflow`, `async_activation_delivery_is_strict_linear_typed_and_dense`, `validated_flow_projects_canonical_actor_task_report_graph`). `static_supervision_proof_budget_is_exact` pins N/N−1 proof admission and `static_supervision_pipeline_polls_late_cancellation_exactly` covers late cancellation. Explicit `img.supervise(...)` fails closed as `semantic-supervision-policy-pending`. Restart policy/intensity, failure/epoch events, actor replacement, teardown/reprovisioning, `PeerFailed` delivery, and assertion abandonment remain absent. | partial — req-10 |
| 7.8 | Reset outcome precision and sibling invalidation. | Not implemented. | gap — req-10 |
| 7.9–7.9.3 | Optional advertised record/replay logs every nondeterministic input/injection/output, bounded policy and divergence; time-travel is tooling over it; cleanup-dependency-graph states (child registration, quarantine, pending/ready cleanup nodes, receipt/recovery transfer, mailbox reopening) exposed as first-class replay events for recovery debugging. | No complete record/replay profile. | gap — req-10 |
| 7.10 | Sealed A/B deployment and versioned persistent migrations; no hot/runtime installation. | Distribution/update proof absent. | gap — req-10,16 |
| 7.11 | Required executable checks/proofs without claiming a general user proof language. | Individual validators exist; complete protocol proofs absent. | partial — req-08,09 |

## Chapter 08 — Build contract

| Spec | Contract inventory | Layers/evidence | Status / delivery owner |
|---|---|---|---|
| 8.1–8.2.1 | Fail-closed source/type/image closure and initialization/copy/move/error conversion proofs. | Implemented for bounded subsets only. | partial — req-02,06,09 |
| 8.2.2 | Complete access/view/region/bounds/linear cleanup proof. | Missing general implementation. | gap — req-05 |
| 8.2.3 | Complete actor/async capacity, ordering, wait, receipt, request, checkpoint and priority proof. | Initial actor send slice only. | gap — req-03,04,07 |
| 8.2.4 | Complete hardware provenance/layout/queue/untrusted/cancellation proof. | Missing general implementation. | gap — req-08 |
| 8.2.5 | Concrete specialization, verified WIR transformations, exact placement and emitted/report reconciliation. | Selected verticals only. | partial — req-09,15 |
| 8.3 | Machine/readable report contains every listed compiler/input/reachability/memory/actor/async/device/recovery/artifact fact; build identity is manifest plus source digests plus the toolchain `core` component version, with no lockfile closure. | `wrela-image-report` exists; required schema is incomplete. | gap — req-09,13 |
| 8.4–8.4.9 | Stable source diagnostics with facts/why paths/repairs for every illustrated category. | Diagnostics infrastructure and selected snapshots; full category coverage absent. | partial — req-13 |
| 8.5 | Tooling exposes all inferred effects and advisory performance shapes. | Partial reports; no complete expanded-effects surface. | gap — req-13 |
| 8.6–8.7 | Byte reproducibility and immutable safety rules across exact build profiles. | Determinism tests exist for slices; full artifacts/distribution unproven. | partial — req-17 |
| 8.8 items 1–8 | Grammar/evaluation/arithmetic/copy/view/carrier/brand conformance corpus. | Scattered subset tests; matrix incomplete. | gap — req-14 |
| 8.8 items 9–17 | Nonwrap cleanup/actors/receipts/wait/requests/capacities/wake/checkpoint corpus. | Initial send/timeout tests only. | gap — req-14 |
| 8.8 items 18–25 | IRQ/poll/vector/ISR/virtio/DMA/reset/wire corpus. | Runtime ABI contract only. | gap — req-14 |
| 8.8 items 26–33 | Comptime/secret/supervision/UEFI/replay/optimization/Unicode/frame/mailbox corpus. | Comptime/cache and some native tests only. | gap — req-14 |
| 8.8 items 34–36 | Report reconciliation, all test modes/protocol failures, ARM64 COFF inspection and pinned QEMU boot. | QEMU smoke/native lanes exist but complete feature corpus absent. | partial — req-14,15 |
| 8.8 items 37–39 | Declaration-header continuation lexing, `##` doc-comment attachment/exposure (and `#` non-attachment), and cleanup-dependency-graph replay-visibility corpus. | Not implemented. | gap — req-13,14 |
| 8.9 | Performance claims require named measured compiler/target/workload and never substitute for semantic proof. | Documentation constraint. | verified — req-13 maintains |
| 8.10 | Conformance requires every applicable chapter 01–10 obligation, not modeled/parsed support. | This inventory records current nonconformance. | verified — documentation |

## Chapter 09 — Optimization model

| Spec | Contract inventory | Layers/evidence | Status / delivery owner |
|---|---|---|---|
| 9.1–9.2 | As-if optimization through distinct verified SW/FW/MW boundaries; UB-bearing backend facts trace to proofs. | Crate boundaries/validators exist for selected operations. | partial — req-15,18 |
| 9.3 | Closed-world monomorphization/dispatch/config/DCE and reproducible declared guidance without optimization-dependent safety. | Analysis interns concrete flat-structure, module-level synchronous function, and concrete read-receiver method specializations by declaration plus ordered scalar type arguments and independently seals their exact HIR substitution, key, receiver, and call target (`generic_read_receiver_method_specializes_and_seals_exactly`). An interface requirement and concrete impl method may carry matching ordered unbounded method-owned type parameters (`concrete_impl_generic_method_specializes`); separately, one primitive-scalar specialization of a generic interface can dispatch on each nongeneric flat receiver after exact requirement-owned substitution (`concrete_generic_interface_specialization_dispatches`). Multiple applications requiring overload selection, generic receiver structures/impl targets, bounds, and nested/nominal arguments remain excluded by named diagnostics. The primitive stored-copy-scalar subset reaches actor SemanticWir for generic aggregate construction/projection and generic free-function signature, body, and direct calls. Lowering authenticates source ownership/kind/order/substitution; SemanticWir sealing rejects function instance-key and call-target forgery (`generic_flat_structure_specialization_lowers_constructor_and_projection`, `generic_copy_scalar_function_specializations_lower_exactly`). Method-owned primitive-scalar generic specializations on nongeneric flat read receivers now retain their exact receiver-first call order in actor SemanticWir (`concrete_read_receiver_method_call_lowers_to_exact_semantic_call`). Generic-interface lowering, async functions, generic structure mutation, generated-test and native specialization proof, broader dispatch, and DCE remain open. | partial — req-09,15 |
| 9.4 | Actor fast paths preserve all nine listed logical admission/scheduling/cancellation/fault/replay properties. | No equivalence suite for general actors. | gap — req-03,17 |
| 9.5 | Generated static scheduler may specialize representation while preserving ISR/device boundaries. | Runtime incomplete. | gap — req-03,04 |
| 9.6 | State-sensitive async frame/continuation optimization preserves cleanup and reports source mapping. | General async absent. | gap — req-04 |
| 9.7 | Ownership-driven optimization and removed checks require FW proof; shifts and hardware effects lower without LLVM UB. | Checked shift vertical exercises a bounded subset. | partial — req-02,15 |
| 9.8 | Physical memory planning preserves logical capacity and proves all overlap across failure paths. | Models only. | gap — req-09 |
| 9.9 | Reports expose logical/physical decisions, proof-backed removed checks and performance diagnostics. | Incomplete report surface. | gap — req-13 |
| 9.10 | Numerical claims require named reproducible benchmark boundary. | Documentation constraint. | verified — req-13 maintains |

## Chapter 10 — Standard library contracts

| Spec | Contract inventory | Layers/evidence | Status / delivery owner |
|---|---|---|---|
| 10.1 | Alternative implementations preserve exact typed effects/bounds/events/diagnostics with no hidden behavior. | Architecture policy; complete library absent. | partial — req-11 |
| 10.2 | Fixed shadowable prelude (`Option`, `Some`, `None`, `Result`, `Ok`, `Err`, `panic`) resolving without import; every other standard name explicitly imported; the set cannot grow without a language revision. | Implemented; see row 2.2.2. | partial — req-02,11 |
| 10.3 | General Option/Result payload ownership, projection carriers, `?`, unique From and mut Option take. | The existing owned-rvalue `Result[S,S]` propagation remains. A new exact scalar `Option[S]` subset authenticates the public `core.option.Option[T]` declaration and fixed-prelude `Some` constructor, then permits postfix `?` only on an owned call/constructor rvalue inside a function returning the identical specialization. `Some` yields its stored copy scalar; `None` reconstructs and returns the same tag-only variant. `ExpressionResolution::OptionTry` retains the distinct payload and propagated value identities; full sealing rejects variant/value substitution (`core_option_scalar_rvalue_question_propagates_none_and_yields_some_payload`, `core_option_question_full_seal_rejects_variant_and_value_substitution`). Named places, wrong enclosing results, and non-core lookalikes remain named fail-closed (`core_option_question_adjacent_ownership_and_identity_tails_fail_closed_by_name`). Analysis values and semantic-lowering operations pin exact N/N−1 bounds plus late cancellation. Semantic lowering independently reauthenticates the core package/module/declaration and expands to an existing closed enum match; Flow lowering authenticates the exact mixed-arity protocol, and `core_option_scalar_question_reaches_deterministic_native_coff` reaches MachineWir and repeat byte-identical native COFF when LLVM is enabled. Non-scalar/owned payloads, `Option` projection carriers, named-place moves, conversion/`ok_or`, unique `From`, and mutating `take()` remain absent or fail-closed. | partial — req-06 |
| 10.4 | Image-wired Actor/Static, exactly-once typed calls/admission/peer failure/ownership-conditioned outcomes composed through the `ActorCallError`/`AdmissionResult` taxonomy, and the `iso[Owner.Brand]` alias spelling. | Unit send across actors: `actor_cross_send_vertical.rs`. Typed payloads, replies, peer failure, ownership-conditioned outcomes, and brand-alias spelling absent. | partial — req-03,07,11 |
| 10.5 | Completion, the single merged sealed `Receipt[P]` state machine with the `@receipt_handoff` calling convention, and ambient `RequestContext[R]` lineage (override-only `request=`; `@detached` opt-out) implement strict exactly-once recovery. | Not implemented end to end. | gap — req-07,11 |
| 10.6 | Complete phase-neutral Duration/Instant/now semantics — one surface, not a runtime/comptime twin pair — with the `as_nanoseconds` method accessor, including record/replay and checked APIs. | Phase-neutral operator-based Duration surface: `std/wrela-core-0.1/src/time.wr` (constructors, `as_nanoseconds`, `scale`, `min`/`max`/`clamp`, `Add`/`Sub`/`Ord` impls; no twins), proven by `stdlib_time_scalar.rs`; `Instant`, `now`, record/replay, and checked-`Result` forms remain absent. | partial — req-11 |
| 10.7 | Static task identities, explicit AsyncExit, idempotent wake and supervised TaskFailed. | Not implemented generally. | gap — req-04,11 |
| 10.8 | Bounded nurseries/joins and capacity-proved race with complete loser teardown. | Not implemented. | gap — req-07,11 |
| 10.9, 10.9.1 | Fixed arrays/List/SlotMap exact capacities, ownership iteration, fresh IDs/generations, exhaustion, and the closed builtin iteration operations (`items`/`items_mut`, `keys`/`values`/`pairs` and `_mut` forms with at most one live element view; no user-defined iteration protocol). | Not implemented generally. | gap — req-06,11 |
| 10.10–10.10.1 | Bounded formatting/Secret/panic plus validated external formats — `Validated[F, T]` as the proof-out instance of the evidence-wrapper family — and exact wire decoding. | Not implemented generally. | gap — req-11 |
| 10.11 | InterruptCell/MMIO/DMA/VirtQueue sealed contracts preserve authority/order/ownership/recovery. | Runtime ABI fragments only. | gap — req-08,11 |
| 10.12 | Image/request/nursery/pool semantic intrinsics preserve exact graph/generativity/cleanup contracts. | Build models only. | gap — req-09,11 |
| 10.13 | `core.ops` operator interfaces (`Add`/`Sub`/`Ord` shapes, read operands, exact desugaring incl. swapped-mapping comparisons with left-to-right evaluation). | `std/wrela-core-0.1/src/ops.wr` and the Duration impls in `time.wr`; comptime-tier operator evaluation proven by `duration_scalar_test.wr` through `stdlib_time_scalar.rs`; native/runtime execution of desugared operator calls and the `Eq`/`Mul` peers remain absent. | partial — req-11 |
| 10.14 | Deriving contracts: `deriving(Eq)` structural equality for non-copy types, `deriving(Format)` bounded formatting, single-variant/single-field `deriving(From)`; hand-implementation permitted under the same obligations. | Executable `Eq` subset: a nonempty nongeneric flat structure with primitive stored copy-scalar fields compares structurally with borrowed operands through deterministic native COFF (`multifield_derived_eq_reaches_deterministic_native_coff`). Sema and lowering independently authenticate the deriving clause, nominal identity, declaration-order field identities, both field projections, every comparison, and the deterministic conjunction chain; `!=` negates only the completed equality. `multifield_derived_eq_full_seal_rejects_order_and_fold_forgeries` rejects reordered fields and aliased comparison/conjunction authority. Empty/recursive/generic/enum equality, aggregate function boundaries, generated method members, `Format`, generated `From`, and hand-implementation equivalence remain open. | partial — req-11 |
| 10.15 | `*_proven` naming convention: every statically-proved variant of a possibly-failing operation carries the suffix and requires its build-time proof context. | Not implemented; no `_proven` API exists in `std/wrela-core-0.1`. | gap — req-11 |
| 10.16 | API naming: CamelCase variant constructors, snake_case functions/methods, `..N` bounded-occupancy versus plain-`N` exact-extent spelling, and unary/positional `_` labeling per §2.4. | Label spelling enforced (row 2.4); naming-style lint still absent. | partial — req-11,13 |

## Normative exclusion ledger

These are the only revision-boundary exclusions. A missing implementation from
the tables above remains a gap even if it is difficult or scheduled later.

| Exclusion | Normative citation |
|---|---|
| Multicore *runtime execution* on the advertised profile (semantic static placement is normative in 01 §8.1; full multi-core boot/GIC beyond a proof vertical is deferred) | 01 §8 |
| Shared-memory concurrency; app-visible atomics | 01 §8 |
| Dynamic application installation/loading, JIT/runtime code generation, runtime reflection/dynamic dispatch/`dyn` | 01 §§2,8; 06 §6 |
| Tracing garbage collection and ambient/unbounded heap allocation | 01 §8; 03 §9 |
| General unsafe blocks and general FFI | 01 §6 |
| Shared legacy PCI INTx | 01 §8; 05 §9 |
| Arbitrary privileged/top-half ISR escape hatches and ISR nesting on the advertised profile | 01 §8; 05 §11 |
| Install-time verified bytecode and hot in-memory migration | 01 §8; 07 §10 |
| Hex floating literals; multiline/raw literals (`"""` is reserved and a syntax error) | 02 §1 |
| 128-bit integers `u128`/`i128` (cut; GUID-shaped data is `Bytes[16]`) | 02 §5.1 fixes the closed scalar set; reversal recorded in design-decisions |
| `class` as a distinct declaration form (merged into `struct` + `linear`) | 02 §3; 02 §3.2 |
| A comptime function color (e.g. `comptime fn`); `fn` is phase-neutral instead | 02 §3.1; 06 §1 |
| `bind` pattern keyword (bare identifiers always bind) | 02 §3.3 |
| Rest/slice patterns | 02 §3.3 |
| Interface specialization, negative impls and overlapping blanket impls | 02 §3.4 |
| Multi-leaf/tuple projection carriers and explicit `from` provenance clauses (provenance is implicit and conservative instead) | 02 §3.5; 03 §4.3 |
| Suspend-safe scope repair tokens and restoration obligations across `await` (scopes cannot hold state across `await`) | 02 §3.5 |
| Variadics/runtime keyword-argument collections | 02 §4 |
| Runtime-indexed fixed-array `take`; loop `else`; labeled `break` | 02 §§6–7 |
| General user-defined iteration protocol (`for` iterates a closed compiler-known set) | 02 §7; 10 §9.1 |
| Wrapping left-shift operator `<<%` (wrapping `+%`/`-%`/`*%` remain) | 02 §8 |
| Arbitrary AST macros | 06 §7 |
| `race` as a grammar/expression form (a sealed stdlib call contract instead) | 04 §12.3; 10 §8 |
| Lockfiles / a locked dependency closure (returns with third-party package acquisition) | 02 §2.1; 08 §3 |
| Service reentrancy / declared interleaving contracts as an alternative to the slot idiom | 04 §13 specifies the slot idiom; reentrancy remains excluded |
| General session-type language | 05 §14 |
| General user-facing design-by-contract/proof language | 07 §11 |
| CST and LSP (product objective; syntax remains a lossless typed AST) | `crates/README.md` and project scope; neither is promised by chapters 01–10 |
| Targets other than `aarch64-qemu-virt-uefi` | 02 §2.1 fixes the only accepted full-image target for revision 0.1 |

## Known unsupported-path register

Every non-exclusion gap is visible in the chapter tables. The highest-risk
accepted-but-not-executed boundaries are tracked explicitly here so a parser or
model cannot be mistaken for language support:

1. General functions/CFG/ABI and aggregate memory execution are not complete
   beyond the named scalar, flat-structure, enum/Result and time verticals.
2. Actors do not yet provide the recurring bounded cross-actor runtime,
   non-reentrant scheduler, complete replies, requests or wait graph.
3. General async state machines, ownership/views/regions/cleanup, hardware
   protocols, recovery/supervision and record/replay are absent end to end.
4. `Option`, general `Result[T,E]`, bounded collections, formatting and most
   standard-library contracts remain incomplete.
5. Image closure/resource proofs/report reconciliation and the reference virtio
   storage appliance do not yet prove the complete target profile.
6. Existing native COFF/QEMU tests prove only their named narrow tuples; they do
   not establish chapter-wide runtime conformance or an installed offline
   distribution.

Until the corresponding row becomes `verified`, unsupported source must be
rejected by the owning phase with a stable source-aware diagnostic. Deleting a
rejection to make a fixture parse is not progress on that row.
