# Linux payload authority schema 1

`representative.lock` is the exact canonical authority derived from the
toolchain schema-1 representative after selecting the required
`aarch64-unknown-linux-musl` host. It is a codec/conformance fixture only: its
synthetic component measurements are not an enrolled payload or execution
evidence.
