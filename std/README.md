# wrela standard library

The language and target runtime sources will live here. Distribution builds copy
the validated standard-library closure into `share/wrela/std`; it is a pinned
part of the toolchain rather than a host dependency.

